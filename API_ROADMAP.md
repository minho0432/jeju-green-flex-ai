# Jeju Green Flex AI API 도입 로드맵

이 문서는 `무엇을 이미 연결했고, 다음에는 무엇을 왜 붙여야 하는가`를 팀이 같은 말로 설명하기 위한 체크리스트입니다.

## 1. 현재 실제로 연결된 API

| API | 현재 용도 | 갱신 | 주의점 |
|---|---|---|---|
| Open-Meteo 오늘·내일 시간별 예보 | 기온·습도·풍속·일사량을 미래 예측 모델에 입력 | 앱에서 30분 캐시 | 과거 발표 예보 원본으로 완전 검증한 것은 아님 |
| KPX 제주계통운영정보 | 오늘의 5분 태양광·풍력·신재생합계·수요·공급능력 표시와 재생에너지 예측 보정 | 앱에서 20분 캐시 | 개발계정 100회/일, 12/12 표본 시간만 사용 |

KPX의 순간 발전값은 MW이고 AI 학습 정답은 시간별 MWh입니다. 따라서 `각 5분 MW × 5/60`을 12개 합쳐 한 시간 MWh로 바꿉니다.

## 2. 다음 API 우선순위

| 순위 | 후보 | 붙이면 생기는 기능 | 지금 바로 못 붙이는 이유 | MVP 처리 |
|---:|---|---|---|---|
| 1 | 한국전력 전기차 충전소 운영정보 | 가까운 충전소의 위치·충전기 종류·운영상태 표시 | 공공데이터포털 페이지가 한전 데이터개방포털로 연결되며 별도 가입·활용신청·인증키 확인이 필요 | 키를 받은 뒤 지도보다 먼저 표/목록으로 연결 |
| 2 | 전국전기차충전소 표준데이터 | API 1 장애 때 사용할 정적 충전소 목록 | 기관별 갱신시점과 상태 실시간성이 일정하지 않음 | 위치·사업자·충전기 종류의 대체 목록 |
| 3 | 충전 세션 합성데이터 | 시작·종료·kWh·이상 세션 로직을 대규모로 시험 | 합성데이터이므로 실제 결제나 실제 사용자 행동 증거가 아님 | 결제·리워드 화면 테스트에만 사용 |
| 4 | 출력제어·HVDC·발전기 정비·한계탄소 | 계통기여·환경성과를 더 직접적으로 평가 | 동일 시간축의 공식 실시간 공개 API를 현재 확인하지 못함 | 값을 만들어 넣지 않고 `확인 필요`로 유지 |

공식 확인 링크:

- KPX 제주 5분 계통정보: https://www.data.go.kr/data/15158505/openapi.do
- 한전 전기차 충전소 운영정보: https://www.data.go.kr/data/15147132/openapi.do
- 전국전기차충전소 표준데이터: https://www.data.go.kr/dataset/15013115/standard.do
- 전기차 충전 세션 합성데이터: https://www.data.go.kr/data/15149540/fileData.do
- 제주 연료원별 발전량: https://www.data.go.kr/data/15138838/fileData.do

## 3. 실제 Green Point 지급에 필요한 것은 공개 API가 아니다

실제 지급은 아래 순서가 모두 확인되어야 합니다.

```text
사용자 추천 수락
→ 충전소·충전기 식별
→ 충전 세션 시작/종료
→ 실제 충전량 kWh 확인
→ 결제 완료 확인
→ 추천시간과 겹친 kWh 계산
→ 보장 포인트 + 실제 성과 보너스 정산
→ 세션 ID를 원장에 기록해 중복지급 차단
```

필요한 자료는 `session_id`, `user_id`, `station_id`, `charger_id`, `start_at`, `end_at`, `charged_kwh`, `payment_status`입니다. 이 정보는 공개 충전소 API만으로 받을 수 없습니다. 충전사업자의 제휴 API와 계약, 사용자 동의, 개인정보 보호가 필요합니다.

따라서 발표 문장은 다음이 정확합니다.

> 현재 MVP는 월 캠페인 예산으로 지급 가능한 Green Point를 역산하고 과거·실시간 전력데이터로 성과 정산 로직을 시뮬레이션합니다. 실제 지급은 충전사업자의 세션·결제 API와 제휴한 다음 단계입니다.

## 4. 다음 구현 체크리스트

- [ ] 한전 데이터개방포털 계정과 충전소 운영정보 API 활용승인 확인
- [ ] 실제 응답 샘플에서 열 이름·단위·호출제한 확인
- [ ] 비밀키는 Streamlit Secrets에만 저장
- [ ] 충전소 목록 캐시와 API 장애용 표준데이터 대체 경로 구현
- [ ] 위치가 가까워도 이용 불가능한 충전기는 추천에서 제외
- [ ] 추천시간과 충전소 운영시간을 함께 검사
- [ ] 실제 세션 연동 전에는 `예약 완료`, `포인트 지급 완료` 버튼을 만들지 않음
- [ ] 제휴 후 세션 ID 중복지급 방지 원장과 월 예산 상한 구현

## 5. 해커톤에서 확정할 범위

이번 제출 범위는 `AI 예측 + 공식 실측 보정 + SOC 제약 충전시간 추천 + Green Point 정책 시뮬레이션`입니다. 충전소 API는 승인키가 준비되면 붙일 수 있는 1순위 확장이고, 실제 결제·지급·충전기 제어는 제휴 전에는 구현했다고 말하지 않습니다.
