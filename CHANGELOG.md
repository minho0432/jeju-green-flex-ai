# 변경 내역

## 실측 도착 기반 예측 보정 재현

- `실시간 보정 재현`을 세 번째 데모 모드로 추가
- 선택 시각까지의 과거 실측만 공개하고 미래 실제값을 차단
- 최근 3시간 실제-예측 편차로 남은 예측을 보정
- 가까운 미래는 크게, 먼 미래는 작게 고치는 감쇠 방식 적용
- 보정 후 Green Score와 충전계획 자동 재계산
- 보정 전·후 예측과 도착 실측을 그래프로 비교
- 공식 제주 실시간 발전량 API 미연동 상태를 화면에 명시
- 5분 MW를 시간별 MWh로 바꾸고 자료 누락률을 표시하는 변환 함수 추가
- 미래값 누수·보정 방향·감쇠·단위 변환 자동검사 5개 추가

## 예산 기반 Green 충전 크레딧 강화본

- 사용자가 보상 단가를 직접 입력하던 구조 제거
- `월 캠페인 예산 ÷ 월 목표 Green Time 충전량`으로 최대 P/kWh 자동 계산
- 기본 가정 300만원 ÷ 10만kWh = 최대 30P/kWh
- 30P를 참여 보장 10P와 최대 성과 보너스 20P로 자동 배분
- 69점과 70점 사이의 급격한 보상 차이를 줄이기 위해 보너스를 0P·10P·20P의 3단계로 변경
- 1P를 다음 충전에서 1원처럼 쓰는 `Green 충전 크레딧`으로 명확화
- 예상·보장·정산 크레딧과 시간별 보너스 단가를 화면에 표시
- 예산 단가와 3단계 정책 단위테스트를 추가해 총 9개 검사

## 개인 EV·Green Point·내일 예보 강화본

### 제품 방향

- 핵심 사용자를 `제주 개인 전기차 사용자`로 확정
- 친환경 시간 선택과 가격 혜택을 두 개의 독립적인 핵심 가치로 분리
- 친환경 시간은 재생에너지 예측, 가격 혜택은 Green Point가 담당하도록 정리
- SMP를 소비자 충전요금이 아닌 전력시장 보조지표로 명확히 제한

### Green Score와 포인트

- 기존 가격 45%·재생에너지 55%에서 `재생에너지 80%·SMP 20%`로 변경
- 원 단위 가상 페이백을 Green Point로 변경
- 기본 정책을 참여 보장 10P/kWh + 실제 성과 20P/kWh로 설정
- 한 번 충전당 총 포인트 상한 1,500P 추가
- `1P=1원`은 실제 계약 전의 체감비용 시뮬레이션이라고 화면과 문서에 표시

### 내일 예보 실험

- Open-Meteo 제주시 내일 시간별 기상예보 연결
- 과거값 없이 시간·날씨만 사용하는 별도 실험용 모델 추가
- `검증된 과거 재현`과 `내일 예보 실험` 화면을 분리
- 내일 예보 모델의 과거 관측날씨 기준 성능과 한계를 별도로 기록
- API 실패 시 검증 모드로 돌아갈 수 있도록 오류 메시지 추가

### 신뢰성

- 자동검사에서 80:20 가중치와 내일 예보용 평가 결과 확인
- 포인트 세션 상한 단위테스트 추가
- 전체 단위테스트를 6개에서 7개로 확대

## 강화본

### AI 검증

- 마지막 20% 한 번 검증에서 `5개 기간 × 각 30일` 시간순 백테스트로 변경
- 월·시간 중앙값 기준 대신 24시간 전, 168시간 전, 두 값 평균의 강한 기준 3개와 비교
- 총 3,600개의 시험시간으로 성능 계산
- 전체 백테스트 예측을 `outputs/backtest_predictions.csv`에 저장

### AI 모델

- 하루 전·일주일 전 SMP와 재생에너지 발전량을 입력 특징으로 추가
- SMP는 강한 과거 기준의 오차를 Extra Trees가 보정하도록 변경
- 재생에너지는 HistGradientBoosting 직접 예측 유지
- 모델 파일에 학습 종료일·검증방법·접근법 메타데이터 저장

### 불확실성

- 과거 검증 오차로 약 90% 예상범위 생성
- 가격 상한과 재생에너지 하한을 사용하는 보수적 `planning_score` 추가
- 중심 예측과 보수적 점수 차이를 `forecast_risk_points`로 표시
- 과거 실제값으로 `actual_green_score`를 계산해 사후 성과 정산 가능

### 충전 최적화

- 점수가 높은 시간 여러 개를 흩어 고르던 방식에서 가장 좋은 연속 구간을 찾는 방식 추가
- 목표 SOC, 충전효율, 충전기 출력, 출발시간을 제약조건으로 검사
- 시간 부족 시 가능한 도달 SOC와 실패 경고 표시
- 보수적 점수를 기본 추천 기준으로 사용

### 리워드

- 단일 가상 페이백을 `참여 보장분 + 성과형 보너스`로 분리
- 예보 오류가 나도 참여 보장분은 유지
- 미래 실제값이 없으면 정산상태를 `pending_actual_data`로 표시
- 과거 재현에서는 실제 점수로 성과 보너스를 정산
- 예상 리워드와 실제 재현 정산 리워드를 분리

### 화면

- 연속 추천시간 표시
- 중심 Green Score와 보수적 점수 비교
- SMP·재생에너지 예측범위와 실제값 그래프
- 참여 보장·예상 보너스·실제 성과 보너스 분리 표시
- 구현한 것과 구현하지 않은 것을 화면에서 공개

### 신뢰성

- `scripts/validate_ai.py` 추가·강화
- 최적화·리워드 핵심 규칙 단위테스트 6개 추가
- GitHub Actions 자동검사 추가
- 모델 설명서와 비전공자용 전체 해설 추가
- 가상환경·비밀키·생성 모델을 제외하는 `.gitignore` 강화
